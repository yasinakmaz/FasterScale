<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FasterScale.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="FasterScale">

    <Grid>
        <!-- Navigation View -->
        <muxc:NavigationView x:Name="NvView"
                             Width="500"
                             MinHeight="300"
                             HorizontalAlignment="Left"
                             IsPaneOpen="True"
                             OpenPaneLength="300"
                             PaneDisplayMode="Left">

            <!-- Arama Kutusu -->
            <muxc:NavigationView.AutoSuggestBox>
                <AutoSuggestBox AutomationProperties.Name="Search"
                                QueryIcon="Find" />
            </muxc:NavigationView.AutoSuggestBox>

        </muxc:NavigationView>

        <!-- İçerik ve PropertySizer -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  PropertySizer (Yan menünün genişliğini ayarlamak için) -->
            <controls:PropertySizer HorizontalAlignment="Left"
                        Binding="{x:Bind ViewPanel.OpenPaneLength, Mode=TwoWay}"
                        Maximum="440"
                        Minimum="52"
                        Visibility="{x:Bind ViewPanel.IsPaneOpen, Mode=OneWay}" />

            <!-- İçerik Alanı -->
            <muxc:Frame x:Name="ContentFrame"/>
        </Grid>
    </Grid>
</Window>
